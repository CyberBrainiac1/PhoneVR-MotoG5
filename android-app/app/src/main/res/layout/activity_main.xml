<?xml version="1.0" encoding="utf-8"?>
<!--
  SPDX-License-Identifier: GPL-3.0-only
  PhoneVR-MotoG5 — activity_main.xml — Connection setup screen
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <!-- App title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_subtitle"
        android:textSize="14sp"
        android:layout_marginBottom="32dp"/>

    <!-- Gyroscope warning banner (hidden if gyro is available) -->
    <TextView
        android:id="@+id/tv_gyro_warning"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFCC00"
        android:padding="12dp"
        android:textColor="#FF000000"
        android:visibility="gone"
        android:layout_marginBottom="16dp"/>

    <!-- Auto-discovery -->
    <Button
        android:id="@+id/btn_find_pc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_find_pc"
        android:layout_marginBottom="8dp"/>

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_marginBottom="8dp"/>

    <!-- Status -->
    <TextView
        android:id="@+id/tv_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/status_idle"
        android:layout_marginBottom="32dp"/>

    <!-- Manual IP entry -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/or_enter_ip"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/et_ip_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/enter_ip_hint"
        android:inputType="textUri"
        android:maxLines="1"
        android:layout_marginBottom="8dp"/>

    <Button
        android:id="@+id/btn_connect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_connect"/>

</LinearLayout>
